package com.dequeue.cashfree.entity;

public enum CommissionType {
    /** Commission is a percentage of order amount. */
    PERCENTAGE,
    /** Commission is a fixed flat amount per order. */
    FIXED
}
